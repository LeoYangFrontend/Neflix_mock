import React, { FC } from 'react';

const Header: FC = () => {
    return (
        <header>
            <img src="Netflix-Logo.png" alt="netflix_logo" />
        </header>
    );
};
export default Header;
